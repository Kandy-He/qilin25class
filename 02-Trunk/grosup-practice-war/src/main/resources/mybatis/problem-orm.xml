<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.grosup.practice.problem">

	<select id="getRandomOne" parameterType="map" resultType="com.grosup.practice.beans.ProblemBean">
	SELECT *
	FROM t_problem
	where 1=1
	<if test="knowledgeKey != null and knowledgeKey !=''">
		and knowledgeKey = #{knowledgeKey}
	</if>
	<if test="quesTypeKey != null and quesTypeKey !=''">
		and quesTypeKey = #{quesTypeKey}
	</if>
	order by rand() LIMIT 1;
	</select>
	<!-- 根据题目Key取题目答案 -->
	<select id="getAnswerByKey" parameterType="java.lang.String" resultType="java.lang.String">
		select tpr.answer from t_problem tpr where tpr.problemKey = #{problemKey}
	</select>
	<!-- 根据题目Key取题目  -->
	<select id="getProblemByKey" parameterType="java.lang.String" resultType="com.grosup.practice.beans.ProblemBean">
		select tpr.* from t_problem tpr where tpr.problemKey = #{problemKey}
	</select>
</mapper>